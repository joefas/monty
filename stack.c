#include "monty.h"
/**
 * f_stack - prints the top
 * @counter: line number
 * @head: stack head
 * Return: 0 always
 */
void f_stack(stack_t **head, unsigned int counter)
{
	counter(void);
	head(void);

	tap.lifi = 0;
}
